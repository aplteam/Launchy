 r←GetCommandLineParms
⍝ Returns the command line parms, if any.
⍝ There are potentially two sources:
⍝ * INI entry "CommandLine"
⍝ * User definition entered into the GUI field "Command line paramaters"
 :If 0=+/n.cmdLineArgs.SelItems
     r←n.cmdLineArgs.Text
 :Else
     r←n.cmdLineArgs.((SelItems⍳1)⊃Items)
 :EndIf
 :If 0<Ini.CONFIG.⎕NC'CommandLine'
 :AndIf 0<≢Ini.CONFIG.CommandLine
     r,←⊃,/' ',¨Ini.CONFIG.CommandLine
 :EndIf
 r,←n.DyalogWSLoadedStop.State/' DYALOGWSLOADEDSTOP=1'
⍝Done
