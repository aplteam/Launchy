 ShowCmd←{
⍝
     (parms adminFlag)←⍵
     m←⊂'Want to execute:'
     m,←⊂parms.file
     m,←' 'U.Split U.dmb parms.lpParms
     m,←(~adminFlag){(0∊⍴⍵):'' ⋄ ⊂('in ',⍵,' ',⍺/'?')}parms.lpDirectory
     m,←adminFlag/⊂'with admin rights?'
     ⍺:WantToExecute m
     1
 }
