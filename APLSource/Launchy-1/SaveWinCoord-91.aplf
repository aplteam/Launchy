 SaveWinCoord←{
 ⍝ Save the current values of globals defining the coordinates of Edit windows and
 ⍝ possibly also Tracer windows (depending on user's choice)
     rp←{0∊⍴⍵:GetRegPathForCurrentDyalogVersion'' ⋄ GetRegPathForDyalog,⍵}⍵
     _←R.PutString(rp,'\edit_cols')(⍕G.ED_Cols)
     _←R.PutString(rp,'\edit_rows')(⍕G.ED_Rows)
     _←R.PutString(rp,'\edit_first_x')(⍕G.ED_FirstX)
     _←R.PutString(rp,'\edit_first_y')(⍕G.ED_FirstY)
     ⍬
 }
