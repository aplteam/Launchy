 GetRegPathForCurrentDyalogVersion←{
⍝ Returns full registry path for the currently selected version
⍝ of Dyalog APL in case  ⍵ is empty or ⍵ otherwise (probably the default version)
     v←{0∊⍴⍵:WhichAPL ⋄ ⍵}⍵
     GetRegPathForDyalog,v
 }
